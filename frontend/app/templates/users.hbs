<div class="col-xs-4">
  <div class='gradient-box'>
    <ul>
      {{#each model as |user|}}
      <li>
        {{link-to user.name 'users.user' user}}
      </li>
      {{/each}}
    </ul>
  </div>
</div>
<div class="col-xs-8">
  {{outlet}}
</div>
